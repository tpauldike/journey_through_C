#include <stdio.h>
#include <stdlib.h>

typedef struct node {
	int data;
	struct node *parent;
	struct node *left;
	struct node *right;
} btr;

int main(void)
{
	btr *root;

	root = malloc(sizeof(btr));
	root 

	

	return (0);
}
